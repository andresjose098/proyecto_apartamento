<h1 class="h4 mb-4">Subir documento</h1>

<%= form_with model: @document, local: true, html: { multipart: true } do |f| %>
  <div class="mb-3">
    <%= f.label :title, "Título" %>
    <%= f.text_field :title, class: "form-control", required: true %>
  </div>
  <div class="mb-3">
    <%= f.label :file, "Archivo" %>
    <%= f.file_field :file, class: "form-control", required: true %>
  </div>
  <%= f.submit "Guardar", class: "btn btn-primary" %>
  <%= link_to "Cancelar", documents_path, class: "btn btn-link" %>
<% end %>
